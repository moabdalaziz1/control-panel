module.exports = {
  plugins: [
    [
      'postcss-preset-env',
      {
        // Options
      },
    ],
  ],
};

// All Postcss Modules is used to support all browsers automatically with prefixes like ( -webkit-) etc..

/* { Postcss-preset-env } is used to transform all modern css code into traditional code
  that all browsers can UnderStand and also used to add Autoprefixer */
